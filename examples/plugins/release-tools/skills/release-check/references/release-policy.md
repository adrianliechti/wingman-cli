# Release policy

- A release must start from a clean working tree.
- The version in the manifest, changelog heading, and proposed tag must agree.
- Treat skipped checks as blockers unless the user explicitly accepts them.
- Verification never creates or pushes the tag; that remains a separate,
  deliberate action.
