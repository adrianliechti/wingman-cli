{{- if .PlanFile -}}
{{- if .PlanMode}}
The active session plan file is `{{.PlanFile}}`. Plan mode is enforced by the tool layer: you may only modify this file while planning. Keep it current as you explore, and revise it instead of rewriting the plan from scratch in chat.
{{- else}}
The active session plan file is `{{.PlanFile}}`. If the user is executing an approved plan, consult it and keep it aligned when scope changes.
{{- end}}

Current session plan:

```markdown
{{if .PlanContent}}{{.PlanContent}}{{else}}(empty){{end}}
```
{{- end}}
